### MySQL底层使用B+树存储

B+树：
- 数据只存在叶子节点，内部节点只存索引键
- 所有叶子节点通过双向链表连接，支持高效的范围扫描与顺序遍历
MySQL为什么选用B+树：
- 范围查询高效
- 磁盘IO更少 { 内部节点不存数据只存键值 需要的磁盘读写次数更少 }
- 支持排序和分组（叶子节点天然有序）
- 全表扫描友好 （沿着叶子节点链表顺序读取即可顺序完成全表扫描，比随机IO快得多） 